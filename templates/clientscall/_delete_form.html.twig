<form method="post" action="{{ path('app_clientscall_delete', {'id': clientscall.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clientscall.id) }}">
    <button class="btn">Delete</button>
</form>
